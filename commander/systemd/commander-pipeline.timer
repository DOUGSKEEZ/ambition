[Unit]
Description=Run the Commander pipeline daily at 07:15 (local time)

[Timer]
# 15 min after UAV (07:00) so the two morning fetches don't overlap.
OnCalendar=*-*-* 07:15:00
Persistent=true

[Install]
WantedBy=timers.target
