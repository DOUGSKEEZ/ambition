# support service — personal network (friends, former colleagues, referral sources).
# Owns network_people; READS people + companies (name/title/photo enrichment joined by
# linkedin_slug) — never writes them. Keyless: no AI at all, by design.
PORT=7707

# Shared database — the same Postgres DB Sniper + Medic + Engineer + SpecOps + UAV + Commander use.
DATABASE_URL=postgres://samwise:samwise123@localhost:5432/sniper

# Sniper's media dir (contact photos) so linked people render with their picture.
SNIPER_MEDIA_DIR=../sniper/media
